interface:
  display_name: "Reset Project Memory"
  short_description: "Clear all memory for this project"
  default_prompt: "Use $reset-project to completely clear the current project's memory."
policy:
  allow_implicit_invocation: false
